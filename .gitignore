*.log
.DS_Store
.env
.env.backup
.env.production
.env.hostinger
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail
Homestead.json
Homestead.yaml
Thumbs.db

# --- Deploy langsung dari GitHub ---
# vendor/ & public/build SENGAJA di-commit agar `git pull` di Hostinger langsung jalan
# tanpa perlu composer install / npm run build di server.
# (Baris "/vendor" dan "/public/build" sengaja DIHAPUS dari daftar di atas.)

# File deploy berisi rahasia (APP_KEY / password admin) — JANGAN pernah di-commit:
/database-schema.sql
/DEPLOY-HOSTINGER-NOCLI.md
